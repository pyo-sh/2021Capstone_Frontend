import React from "react";
import { StyleSheet, View, Text } from "react-native";

const FridgeContent = ({}) => {
	return <View style={styleSheet.wrapper}></View>;
};

const styleSheet = StyleSheet.create({
	wrapper: {}
});

export default FridgeContent;
